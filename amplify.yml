version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn build:types
    build:
      commands:
        - yarn run build:amplify
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --production=false
    build:
      commands:
        - yarn run build:amplify
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

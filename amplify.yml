version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn build:types
    build:
      commands:
        - nvm use $VERSION_NODE_14
        - yarn run build:amplify
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
